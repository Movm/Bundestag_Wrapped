packages:
  - apps/wrapped
  - apps/wrapped/mobile
  - services/mcp
  - packages/*
allowBuilds:
  better-sqlite3: true
  esbuild: true

# NOTE: services/analysis is a Python project (pyproject.toml) — intentionally
# NOT a pnpm workspace member. See README / CLAUDE.md.
