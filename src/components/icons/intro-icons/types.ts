/**
 * Shared props interface for all intro icons
 */
export interface IntroIconProps {
  /** Tailwind classes for sizing (default: w-24 h-24) */
  className?: string;
  /** Primary gradient color (hex) */
  primaryColor?: string;
  /** Secondary gradient color (hex) */
  secondaryColor?: string;
}
