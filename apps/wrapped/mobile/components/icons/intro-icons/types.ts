/**
 * Shared props interface for all intro icons (React Native)
 */
export interface IntroIconProps {
  /** Icon width in pixels */
  width?: number;
  /** Icon height in pixels */
  height?: number;
  /** Primary gradient color (hex) */
  primaryColor?: string;
  /** Secondary gradient color (hex) */
  secondaryColor?: string;
}
